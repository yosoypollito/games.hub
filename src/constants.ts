import { Monoton, DM_Sans } from "next/font/google";

export const monoton = Monoton({ weight: "400", subsets: ["latin"] });
export const dmsans = DM_Sans({ weight: ["400", "500", "700"], subsets: ["latin"] });